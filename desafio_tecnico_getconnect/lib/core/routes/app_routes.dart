abstract class AppRoutes {
  // Durante o desenvolvimento pensei em unir register e login a uma única view (Por conta de compartilharem praticamente os mesmos widgets)
  // mas decidi em separar por conta da descrição da documentação (Tela de cadastro com nome ... Tela de login com e-mail)
  static const login = '/login';
  static const register = '/register';
  static const chat = '/chat';

}